# Feishu Publish QA Rules

这个模块只负责交付闭环：

- 文档映射
- 新建 / 覆盖发布
- 链接替换
- 图片与图位
- 回读
- QA

只有当 `publish_gate` 已通过，且 acceptance panel 全员 > 8.0 时，才允许进入发布。

它不负责：

- 选题
- 结构设计
- 写稿
- 做行业判断

工程要求：

- 可重跑
- 最小权限
- 失败定位清晰
- 发布失败要明确定位到文档、链接、图片或锚点
